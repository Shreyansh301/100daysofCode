class Solution {
    public int[] getConcatenation(int[] nums) {
        final int n = nums.length;

        int[] ans = new int[n*2];

        for (int i = 0; i<n; ++i)
        ans[i] = ans[i+n] = nums[i];

        return ans;
    }
}

LOGIC: Explanation 
Example - Input: nums = [1,2,1]
Output: [1,2,1,1,2,1]

This Java code defines a class Solution with a method getConcatenation that takes an array of integers (nums) as input and returns a new array that is the concatenation of the input array with itself.
